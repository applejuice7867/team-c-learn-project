[project]
name = "edupulse-worker"
version = "0.1.0"
description = "EduPulse FastAPI on Cloudflare Python Workers"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "pydantic"
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]